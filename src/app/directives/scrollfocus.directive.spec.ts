import { ScrollfocusDirective } from './scrollfocus.directive';

describe('ScrollfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollfocusDirective();
    expect(directive).toBeTruthy();
  });
});
